# 首次运行未完成，禁止用于完整队列排名

本次在 2026-09-23 执行 `native development`、三进程、两候选族、七算法的开发比较。记录到 109/290 个任务组、1708 条预算结果时，Windows 并发发布同一缓存的 `temporary.replace(path)` 抛出 `PermissionError: [WinError 5]`。任务池继续完成已提交计算后才退出，后续结果未汇总；没有 `completion.json`，不能视为完整实验。

错误发生在 `lower_limb_sim/myoleg_benchmark/simulation.py` 的缓存文件发布，与算法判定的力学可行性不同。本目录的原协议和中间文件原样保留，不补写、筛选或拼接为成功结果。

修复使用同目录硬链接原子发布，已存在目标经内容核对后复用，不覆写其他进程正在读取的缓存。运行器同时新增即时任务错误登记和明确的不完整完成状态。修复后另起完整重跑目录 `../development_20260923_r2/`；缓存身份含模拟器源码，因此该次修复不沿用旧身份的缓存。评分、候选域、方法参数、主体、预算和随机种子保持相同。

后续分析仅使用完成且通过协议覆盖检查的重跑结果；本目录只保留工程故障追踪用途。
